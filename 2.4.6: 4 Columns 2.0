"""
Bu program ekrani 4 esit parcaya bölen yatay cizgiler cizer.
"""


penup()
backward(200)
left(90)
forward(200)

for i in range(2):
	right(90)
	penup()
	forward(100)
	right(90)
	pendown()
	forward(400)
	left(90)
	penup()
	forward(100)
	left(90)
	pendown()
	forward(400)
